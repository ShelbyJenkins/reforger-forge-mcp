# Local observer failure matrix — assertion migration ledger

**Status:** Retention inventory only; zero assertions have been behaviorally
replaced or removed. Deliberate native mutation review remains outstanding.

This ledger is the migration table required by the retained failure-matrix
plan. It classifies the source-reading contracts reviewed during the runtime
pilot and the implemented 69-case Workbench harness. `Pending` now refers to a
missing deliberate native mutation or reviewed live result, not a missing
Workbench catalog, hook, or hermetic seam. The assertion remains in the
ordinary suite; a similarly named test is never permission to delete it.

## Current outcome

- Behavioral replacements: **0**
- Static assertion groups removed: **0**
- Assertion groups retained: **22**
- Retained checks retargeted after ownership extractions: **recorded inline;
  no migration credit**

Source-reading groups are counted by their owning test/invariant, not by every
implementation file that the test reads. Retargeting a retained check across
the extracted runtime, live-case, or dispatcher owner does not make it a
behavioral replacement.

Accordingly, this document records why the current static rules still exist;
it does **not** claim that Task 6 produced an assertion migration. The detailed
rows are retained only for the plan's per-invariant accounting requirement.
Task 6 remains open until deliberate mutations and the corresponding reviewed
native cases justify at least one replacement, or the maintainer explicitly
accepts a retention-only closeout.

| Current assertion group | Protected invariant | Behavioral replacement | Deliberate mutation and command | Observed failure | Disposition | Retention rationale |
| --- | --- | --- | --- | --- | --- | --- |
| `tests/observer/runtime-camera-restoration-contract.test.ts` — held-lease cancellation restores the runtime camera | A terminal cancellation cannot leave the runtime camera lease held or restoration unconfirmed. | `runtime.cancel_capture.lease_acquired.pose`, including its public terminal result and mandatory follow-up current capture. | Bypass the fixture build's normal restoration confirmation and run `npm.cmd run dev:observer:acceptance:runtime -- --only runtime.cancel_capture.lease_acquired.pose --confirm-live-run` under the current version-3 evidence contract. | A prior gated pilot produced a passing local version-2 artifact without the deliberate mutation; no version-3 native mutation result is retained. | **Retain** | The hermetic runner proves orchestration, but only Enfusion can prove the native camera was restored. |
| `tests/observer/runtime-camera-restoration-contract.test.ts` — read-only leaseability, resolving, and canonical phase barriers | `CanAcquire` remains read-only; exactly one resolve and one spawn precede the mutating acquisition; cancellation/deadline checks and all five matrix barriers retain their required order around resolving and acquisition. | Runtime scheduler/control-channel tests plus the declared `runtime.cancel_capture.lease_acquired.pose` pilot; resolving/before-lease native cases remain future replacements if they are safely declared. | Remove `CanAcquire`, add a mutation to it, duplicate resolve/spawn, or move an acquisition/barrier; run `npm.cmd test -- tests/observer/runtime-camera-restoration-contract.test.ts tests/observer/fault-control-channel.test.ts`, then the declared pilot or a future affected selected runtime case. | No deliberate source or native mutation result has been recorded; the mutation proof is pending. | **Retain** | Read-only authorization and exact barrier placement cannot be inferred from a successful terminal result, while native execution is still required to prove that the declared phase was actually reached. |
| `tests/observer/runtime-camera-restoration-contract.test.ts` — post-frame ordering and world/camera ownership revalidation | Restoration callbacks precede capture commit/destruction, restoration occurs through the real post-frame path, and cleanup revalidates the exact world, camera slot, and owner. | Future runtime `restoration_in_progress` cancellation and world-loss cases. | Temporarily report restoration after capture commit or before post-frame confirmation, destroy the observer early, then separately relax world/slot/owner binding; run the matching selected live cases. | No deliberate source or native mutation result has been recorded; the corresponding native cases remain undeclared and the mutation proof is pending. | **Retain** | These ordering and ownership invariants are not safely observable through the current lease-acquired pilot or a TypeScript fake. |
| `tests/observer/runtime-live-acceptance-contract.test.ts` — exact owned lifecycle and no direct kill | Runtime shutdown uses `OwnedRuntimeManager.stop`, proves the exact identity vacant, and never selects a process by PID/name. | Hermetic failure-matrix runner call-order tests plus every future owned-shutdown live case. | Replace the owned stop port with a direct-stop fake and run `npm.cmd test -- tests/observer/runtime-failure-matrix-runner.test.ts`; later repeat against an owned-shutdown live case. | No deliberate source or native mutation result has been recorded; the owned-shutdown native family remains undeclared and the mutation proof is pending. | **Retain** | Absence of a forbidden process-control surface is an authorization boundary, not something a successful graphical run can prove. |
| `tests/observer/runtime-live-acceptance-contract.test.ts` — launch arguments and external-root ownership | Matrix mode uses the shared launch-support owner for `-server` and rejects repository/project-owned evidence roots. | Exported launch-support behavior and the live pilot preflight. | The assertions formerly aimed at the monolithic runner were moved to the extracted `observer-runtime-launch-support.ts` owner; run `npm.cmd test -- tests/observer/runtime-live-acceptance-contract.test.ts`. | No deliberate mutation run has been recorded; the assertion was relocated without deletion and its mutation proof is pending. | **Retain at new owner** | This was a source-owner relocation caused by the circular-import split, not a behavioral replacement. |
| `tests/observer/runtime-live-acceptance-contract.test.ts` — managed public workflow and evidence ownership | The runtime acceptance path uses the public begin/capture/instances/finalize workflow, exact-owned lifecycle service, external evidence root, standardized unreviewed bundle, and cleanup only after exact vacancy settles. | Runtime application/service tests, hermetic failure-matrix runner tests, and the selected live pilot bundle. | Replace a public application call with a private-agent call, write an image outside the managed bundle, or move cleanup before the vacancy seal; run `npm.cmd test -- tests/observer/runtime-live-acceptance-contract.test.ts tests/observer/runtime-failure-matrix-runner.test.ts`, then the selected pilot. | No deliberate source or native mutation result has been recorded; the mutation proof is pending. | **Retain** | A passing capture does not prove that an unused private path, premature cleanup branch, or unreviewed evidence bypass is absent. |
| `tests/observer/runtime-live-acceptance-contract.test.ts` — positive-path procedure, labels, diagnostics, and operational-baseline ordering | The retained runtime positive procedure keeps its explicit pose/look-at and post-restoration labels, diagnostic-only similarity policy, configuration/source identities, supervised-exit settle, scratch cleanup, and before-launch/after-shutdown process samples in the reviewed order. | Positive-path helper tests, evidence-bundle validation, operational-baseline tests, and the selected live pilot. | Remove or reorder a capture label, turn similarity into an acceptance gate, move scratch cleanup before supervised vacancy, or omit a baseline identity/sample; run `npm.cmd test -- tests/observer/runtime-live-acceptance-contract.test.ts tests/workbench/observer-live-acceptance-support.test.ts`, then the selected pilot. | No deliberate source or native mutation result has been recorded; the mutation proof is pending. | **Retain** | One passing image or terminal result cannot prove the complete retained procedure, diagnostic-only policy, or negative ordering constraints. |
| `tests/observer/runtime-live-acceptance-contract.test.ts` — production launcher neutrality and matrix CLI delegation | Production observer modules remain launcher-neutral; `--help`/`--list-cases` return before live authorization; `--only` delegates to the matrix runner; and `--keep-profile` is legal only with one selected case. | CLI parser/runner tests, exported launch-support behavior, and gated selected-case execution. | Import the runtime launcher from production, move list/help behind authorization, bypass `runRuntimeFailureMatrix`, or relax the keep-profile gate; run `npm.cmd test -- tests/observer/runtime-live-acceptance-contract.test.ts tests/observer/runtime-failure-matrix-runner.test.ts`. | No deliberate mutation run has been recorded; the mutation proof is pending. | **Retain** | Launcher neutrality and early-return authorization are absence/order constraints that a successful selected live case cannot establish. |
| `tests/workbench/observer-handler-contract.test.ts` — five-handler observer inventory and generic execution ban | The observer capture surface registers only Ping, Submit, Status, Cancel, and Release and never exposes `ExecuteAction`, `RunCmd`, or `RunProcess`; this does not describe unrelated pre-existing helper APIs. | Package/observer-handler inventory checks; no live fault case is allowed to exercise a forbidden observer surface. | Add a sixth observer handler or a generic executor registration to the observer capture surface and run `npm.cmd test -- tests/workbench/observer-handler-contract.test.ts`. | No deliberate mutation run has been recorded; the mutation proof is pending. | **Retain** | Safely observing the forbidden surface at runtime would require shipping or invoking the very capability the rule prohibits. |
| `tests/workbench/observer-handler-contract.test.ts` — camera snapshot, request binding, restoration, and idempotent release literals | Workbench capture snapshots the exact editor transform/projection/slot/viewport/world/project, cross-binds every state-changing request to generation/target/job/handler lease, restores the camera, and releases only after restoration. | Hermetic adapter/fixture tests plus the declared cancellation, world-loss, restoration-in-progress, and terminal-release families. | Temporarily skip `RestoreJob`, relax a generation/target/job/lease or camera identity check, and make the identical release replay disagree; run the handler/adapter tests and each affected selected live case. | No deliberate source or native mutation result has been recorded; the mutation proof is pending. | **Retain** | Host fakes prove choreography and fail-closed projection, but only Workbench can prove the native editor-camera mutation and helper implementation. |
| `tests/workbench/observer-handler-contract.test.ts` — native profile PNG request path and screenshot API | Capture derives one job-bound path beneath the generated native profile capture directory, records its `.png` logical output, calls `System.MakeScreenshot`, and exposes no generic editor execution/save/reload route. | Managed artifact validation tests, fixture capture tests, positive Workbench acceptance, and completed matrix rows. | Change the screenshot path outside the profile root, stop binding it to the job, replace `System.MakeScreenshot`, or add a generic execution route; run `npm.cmd test -- tests/workbench/observer-handler-contract.test.ts tests/workbench/observer-adapter.test.ts`, then one selected completed native case. | No deliberate source or native mutation result has been recorded; the mutation proof is pending. | **Retain** | A valid returned PNG cannot prove its native request path ownership or the absence of an unused generic editor capability. |
| `tests/workbench/observer-live-acceptance-contract.test.ts` and `tests/workbench/observer-acceptance-runtime.test.ts` — local gate, shared disposable composition, and no global kill | Workbench acceptance remains opt-in, creates fresh project/profile state for each matrix case, composes positive and matrix clients/guards/applications only through the shared acceptance runtime, and shuts down only through the exact-owner client/guard. | Authorization and shared-composition ownership tests, serial full/partial matrix orchestration, the exact-exit seal, and selected Workbench live cases. | Remove the environment/confirmation gate, duplicate `WorkbenchClient`/guard/application construction in either orchestrator, or introduce a direct process-kill call; run `npm.cmd test -- tests/workbench/observer-live-acceptance-contract.test.ts tests/workbench/observer-acceptance-runtime.test.ts tests/workbench/failure-matrix-runner.test.ts`. | No deliberate source or native mutation result has been recorded; no retained native Workbench matrix or exact-exit result exists, so the mutation proof is pending. | **Retain** | A live success cannot prove that an unused global-kill branch or duplicate composition path is absent. |
| `tests/workbench/observer-live-acceptance-contract.test.ts` — positive pose/look-at procedure and operational-baseline ordering | The retained positive path applies the reviewed pose/look-at offsets and FOV checks, force-update launch identity, procedure revision/labels/world, representative Ping measurement, supervised-exit settle, and before-launch/after-shutdown process-sample order. | Camera-matrix helpers, positive acceptance artifact validation, and operational-baseline tests. | Remove a pose/look-at check or label, change the world/procedure identity, omit the representative call, or move the final process sample before exit settle; run `npm.cmd test -- tests/workbench/observer-live-acceptance-contract.test.ts tests/workbench/observer-live-acceptance-support.test.ts`. | No deliberate source or native mutation result has been recorded; the mutation proof is pending. | **Retain** | A successful screenshot cannot establish that the full reviewed positive procedure and its negative process-ordering constraints were the path executed. |
| `tests/workbench/observer-live-acceptance-contract.test.ts` — managed run and bundle workflow | Workbench screenshots flow only through begin/capture/status/read/finalize/close, with private-agent transport hidden behind the application and only a standardized unreviewed managed bundle exported. | Observer application/evidence-service tests, hermetic runner bundle tests, and every positive or matrix live bundle. | Replace a managed capture/read with direct adapter artifact access, write an image directly, or omit the unreviewed marker; run `npm.cmd test -- tests/workbench/observer-live-acceptance-contract.test.ts tests/workbench/failure-matrix-runner.test.ts`. | No deliberate mutation run or reviewed native bundle has been recorded; the mutation proof is pending. | **Retain** | A valid image cannot reveal whether a private transport or unmanaged artifact-writing bypass remained available. |
| `tests/observer/live-acceptance-ci-isolation.test.ts` — graphical acceptance absent from CI | The ordinary CI workflow neither sets either live-acceptance authorization variable nor invokes the runtime or Workbench graphical acceptance commands. | CI workflow/package gates and the explicit local authorization checks in both runners. | Add either live environment variable or dev acceptance command to `.github/workflows/ci.yml`; run `npm.cmd test -- tests/observer/live-acceptance-ci-isolation.test.ts`. | No deliberate mutation run has been recorded; the mutation proof is pending. | **Retain** | The invariant is the absence of a dangerous CI capability; safely running ordinary CI cannot prove that an unused graphical step is absent. |
| `tests/workbench/failure-matrix-enforce-contract.test.ts` — passive hooks, authenticated fixture schedule, and phase predicates | Production hooks stay default-inert; only the generated fixture accepts the complete declared vocabulary, one-shot capability/lifecycle binding, and real product-state phase predicates, with no fixture process-control path. | Fixture authorization/control tests, scheduler tests, all 69 dispatcher rows, and the corresponding selected native cases. | Make a production hook active by default, bypass `IsDeclaredSchedule` or `BarrierStateMatches`, or add fixture termination text; run `npm.cmd test -- tests/workbench/failure-matrix-enforce-contract.test.ts tests/observer/fault-control-channel.test.ts tests/workbench/failure-matrix-runner.test.ts`, then the matching selected live case for native phase mutations. | No deliberate mutation run has been recorded; both the source and reviewed native phase mutation proofs are pending. | **Retain** | Enforce hook placement and the absence of an undeclared fixture capability are source authorization boundaries; native Workbench is still required to prove actual phase arrival. |
| `tests/workbench/failure-matrix-runner.test.ts` — runner/decoy safety and source-closure source rules | The runner hashes its support/fixture closure, uses only command-local Git safe-directory arguments, contains no direct Workbench kill, and keeps the decoy helper repository-only. | Source-bracket mutation tests, exact-owner/decoy behavioral tests, package exclusions, and owned-shutdown live rows. | Remove a closure member, replace command-local Git configuration with a global mutation, add a direct kill, or package the decoy; run `npm.cmd test -- tests/workbench/failure-matrix-runner.test.ts tests/observer/package-contract.test.ts` and `npm.cmd run test:package`. | No deliberate mutation run has been recorded; the source proof and native decoy review are pending. | **Retain** | A successful run cannot prove that an unused global-kill branch or omitted source-closure member is absent. |
| `tests/workbench/failure-matrix-support.test.ts` — sentinel-only decoy process | The decoy has one unique owner argument, exits only after its generated sentinel appears, and contains no IPC/signal-triggered exit, kill, process-selection, or other alternate control path. | Decoy launch/exact-identity comparison tests, package exclusions, and the live owned-shutdown comparison rows. | Add a signal/IPC exit path, remove the unique owner argument, or permit exit without the sentinel; run `npm.cmd test -- tests/workbench/failure-matrix-support.test.ts tests/workbench/failure-matrix-runner.test.ts`. | No deliberate mutation or reviewed native decoy result has been recorded; the mutation proof is pending. | **Retain** | A decoy surviving one run cannot prove that an unused alternate control path is absent from its source. |
| `tests/observer/package-contract.test.ts` — published files, generated payloads, manifests, `.gitattributes`, and configuration shape | The installed package contains the reviewed production assets and generated inventories, preserves manifest/config shape, and excludes repository-only failure controls and fixtures. | `npm.cmd run test:package` plus manifest/protocol checks against the packed artifact. | Add a fixture path to `package.json#files`, omit a required payload, or drift a generated manifest; run `npm.cmd run test:package`. | The installed-tarball and inventory gates are the behavioral artifact proof. | **Retain** | These particular checks inspect artifacts and configuration, not implementation source text. |
| `tests/observer/package-contract.test.ts` — `check-package.mjs` and manifest-generator source ownership | Package verification names the required built observer/Workbench closure, rejects deleted/legacy and repository-only paths, derives generated Enforce targets from the contract descriptor without a copied filename list, and uses archive helpers/offline npm without a shell; the manifest generator owns both source manifests and the helper build marker. | Installed-tarball verification, generated-manifest checks, Enforce contract checks, and package exclusion tests. | Remove a required source literal, hard-code a generated Enforce output name, add a forbidden fixture path, reintroduce shell execution, or drop a generated-manifest owner; run `npm.cmd test -- tests/observer/package-contract.test.ts` and `npm.cmd run test:package`. | No deliberate mutation run has been recorded; the mutation proof is pending. | **Retain** | The packed artifact proves the current result but not that the verification/generation scripts retain every future fail-closed ownership rule. |
| `tests/observer/package-contract.test.ts` — exact tool documentation and generated Workbench handler inventory | The public/documented observer surface remains exactly seven tools, while the full Workbench helper inventory is derived from the generated payload descriptor and its Observer capture subset remains exactly five handlers without generic matrix parsing shortcuts; unrelated helper handlers may also exist. | Tool-list/package inventory tests and generated-helper verification. | Add an undocumented tool, hard-code a second handler inventory, or add a sixth Observer capture handler; run `npm.cmd test -- tests/observer/package-contract.test.ts tests/workbench/observer-handler-contract.test.ts`. | No deliberate mutation run has been recorded; the mutation proof is pending. | **Retain** | Exact absence and single-source ownership of a generated/public inventory cannot be proven merely by exercising one valid tool or handler. |
| `tests/observer/package-contract.test.ts` — Phase H composition and dormant/headless/restoration/no-Workbench source invariants | The server composes one observer application/tool surface with ordered disposal; production runtime code remains Workbench-free; the production Workbench adapter contains no repository-only acceptance controls or state; Enforce hooks are dormant and withhold render capability headlessly; restoration and exact session identity literals remain fail-closed. | Application/server lifecycle tests, production-adapter type/source boundary tests, package tests, runtime control tests, and the declared live failure families. | Duplicate composition, import a Workbench controller, move an acceptance control back into the production adapter, activate a hook by default, advertise headless capture, or bypass restoration/identity checks; run `npm.cmd test -- tests/observer/package-contract.test.ts tests/workbench/observer-adapter.test.ts tests/observer/application.test.ts tests/observer/fault-control-channel.test.ts`, then the matching selected native case. | No deliberate source or native mutation result has been recorded; the mutation proof is pending. | **Retain** | These are composition, absence, and authorization invariants; native cases supplement but cannot prove that dormant forbidden branches do not exist. |

## Closeout rule

For each row marked `Retain` because a live mutation is pending, record the
reviewed source revision, selected case ID, exact command, and observed failed
contract before changing the disposition to `Replace`. If the engine cannot
make the unsafe path observable, keep the narrow static rule and replace
`Pending` with the concrete non-observability result. The final closeout must
have no deleted assertion whose invariant is absent from this ledger.
